spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rest_assured_incubadora
    username: ${USER}
    password: 123123
  jpa:
    show-sql: true
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
